@model OMGT_Lab1.Models.Vector

@{
    ViewBag.Title = "Delete";
}

<h2>Delete</h2>

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>Vector</h4>
    <hr />
    <dl class="dl-horizontal"></dl>

    @using (Html.BeginForm())
    {
        @Html.AntiForgeryToken()
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Alternative.AlternativeName)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Alternative.AlternativeName)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.Mark.Criterion.Name)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Mark.Criterion.Name)
        </dd>
        <dt>
            @Html.DisplayName("Mark")
        </dt>
        @if (Model.Mark.Criterion.Type == OMGT_Lab1.Models.Enums.CriteriaType.Quantitative)
        {

            <dd>
                @Html.DisplayFor(model => model.Mark.NumericMark)
            </dd>
        }
        else
        {
            <dd>
                @Html.DisplayFor(model => model.Mark.Name)
            </dd>
        }

    </dl>
        <div class="form-actions no-color">
            <input type="submit" value="Delete" class="btn btn-default" /> |
            @Html.ActionLink("Back to List", "Index")
        </div>
    }
</div>
